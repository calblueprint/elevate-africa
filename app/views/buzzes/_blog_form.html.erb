<% if current_user && current_user.is_admin? %>
  <%= simple_form_for buzz do |f| %>
    <%= f.input :headline %>
    <%= f.input :subhead %>
    <%= f.input :content %>
    <%= f.input :video_link, label: "Embed Youtube Video (optional)" %>
    <%= f.hidden_field :admin_id, value: current_user.id %>
    <!-- Add Image here! -->
    <%= f.submit "Create Post!" %>
  <% end %>
<% end %>
