<%= form_for @campaign do |f| %>
  <% if @campaign.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@campaign.errors.count, "error") %> prohibited this campaign from being edited: </h2>
      <ul>
        <% @campaign.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="campaigns-create-first">
    <div class="campaigns-create-field" id="campaigns-create-field-name">
      <%= f.label :name %><br>
      <%= f.text_field :name, placeholder: "campaign name here", autofocus: true %>
    </div>

    <div class="campaigns-create-field" id="campaigns-create-field-duration">
      <%= f.label :duration %><br>
      <%= f.text_field :duration, placeholder: "duration in days here" %>
    </div>

    <div class="campaigns-create-field" id="campaigns-create-field-goal">
      <%= f.label :goal %><br>
      <span id="campaigns-create-dollar-sign">$</span>
      <%= f.text_field :goal, value: "1,234" %>
    </div>

    <div class="campaigns-create-button" id="campaigns-create-submit-one">Continue</div>
  </div>

  <div class="campaigns-create-second">
    <div class="campaigns-create-example-buttons-container">
      <a class="campaigns-create-example-button" id="campaigns-create-example-one" data-content="Lorem ipsum dolor sit amet, consectetur adipiscing elit, sed do eiusmod tempor incididunt ut labore et dolore magna aliqua. Ut enim ad minim veniam, quis nostrud exercitation ullamco laboris nisi ut aliquip ex ea commodo consequat.">Example 1</a>
      <a class="campaigns-create-example-button" id="campaigns-create-example-two" data-content="Sed ut perspiciatis unde omnis iste natus error sit voluptatem accusantium doloremque laudantium, totam rem aperiam, eaque ipsa quae ab illo inventore veritatis et quasi architecto beatae vitae dicta sunt explicabo.">Example 2</a>
      <a class="campaigns-create-example-button" id="campaigns-create-example-custom" data-content="">Custom</a>
    </div>

    <div class="campaigns-create-field campaigns-create-text-area">
      <%= f.text_area :description, placeholder: "campaign description here" %>
    </div>

    <div class="campaigns-create-button" id="campaigns-create-submit-two">Continue</div>
  </div>

  <div class="campaigns-create-third">
    <div class="campaigns-create-preview-container">
      <img id="campaigns-create-preview" src="<%= asset_path("image_placeholder.png") %>" alt="image preview"/>

      <div id="campaigns-create-picture-options-container">
        <img class="campaigns-create-picture-option campaigns-create-picture-option-left" src="<%= asset_path("create-pic-one.png") %>" onclick="campaignsChoosePicture('one')" alt="image preview" id="campaigns-create-picture-option-one" data-url="create-pic-one.png"/>
        <img class="campaigns-create-picture-option" src="<%= asset_path("create-pic-two.png") %>" onclick="campaignsChoosePicture('two')" alt="image preview" id="campaigns-create-picture-option-two" data-url="create-pic-two.png"/>
        <img class="campaigns-create-picture-option campaigns-create-picture-option-left" src="<%= asset_path("create-pic-three.png") %>" onclick="campaignsChoosePicture('three')" alt="image preview" id="campaigns-create-picture-option-three" data-url="create-pic-three.png"/>
        <img class="campaigns-create-picture-option" src="<%= asset_path("create-pic-four.png") %>" onclick="campaignsChoosePicture('four')" alt="image preview" id="campaigns-create-picture-option-four" data-url="create-pic-four.png"/>
      </div>
    </div>

    <div class="campaigns-create-picture-buttons-container">
      <a class="campaigns-create-picture-button" id="campaigns-create-picture-upload" onclick="campaignsUploadPicture()">
        Upload
        <p>your own photo</p>
      </a>
      <a class="campaigns-create-picture-button" id="campaigns-create-picture-choose" onclick="campaignsChooseOptions()">
        Choose
        <p>one of ours</p>
      </a>
    </div>

    <div id="campaigns-create-hidden-upload">
      <%= f.file_field :picture, id: "campaigns-create-upload" %>
    </div>
    <% if !current_user %>
      <div class="campaigns-create-fourth">
        <%= f.simple_fields_for @team do |t| %>
          <%= t.input :name %>
          <%= t.input :email %>
          <%= t.input :password %>
          <%= t.input :password_confirmation %>
        <% end %>
      </div>
    <% else %>
      <% if current_user && current_user.team? %>
        <%= f.hidden_field :team_id, value: current_user.id %>
      <% end %>
    <% end %>

    <%= hidden_field_tag :picture_choose, "", id: "campaigns-create-hidden-picture" %>

    <%= f.submit "Finish", class: "campaigns-create-button" %>

  </div>

<% end %>
